"""
Tests for field computation.

The verification loop lives here — the 'does the physics behave
correctly?' checks (e.g. a particle in a uniform field should trace
a CLOSED circle; if it spirals open, integration has energy drift).

TODO: add checks as each piece is built.
"""